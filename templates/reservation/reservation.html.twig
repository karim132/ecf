{% extends 'base.html.twig' %}

{% block title %}Réservation{% endblock %}

{% block body %}


    <h4 class='text-center'>Vous êtes sur le point de réserver</h4>
    <h5 class='text-center'>Quelles dates vous intéressent ?</h5>

    <div class="container mt-3">
    
        {{ form_start(form) }}
        {{ form_widget(form) }}
 
        <button type="submit" class="btn btn-primary mt-2">Envoyer</button>
        {{ form_end(form) }}
    </div>





        {# {{ form_start(form) }}
        {{ form_row(form.dateDebut) }}
        {{ form_row(form.dateFin) }}
        {{ form_row(form.salle) }}
        {% for item in form.user %}
        <td>{{ item.user }}</td>
        {% endfor %}
         {{ form_row(form.user) }} 
        <button type="submit" class=" btn btn-primary btn-lg mt-2">Envoyer</button>
        {{ form_end(form) }} #}


{# <form class="row gx-3 gy-2 align-items-center">

  <div class="row mb-3">
    <label for="inputEmail3" class="col-sm-2 col-form-label">Date de début</label>
    <div class="col-sm-10">
	  <input type="date" class="w-50 form-control" id="start" name="trip-start" value="" min="2023-01-01" max="2024-12-31">
    </div>

	  <div class="row mt-3">
    <label for="inputEmail3" class="col-sm-2 col-form-label">Date de fin</label>
    <div class="col-sm-10">
      
	  <input type="date" class="w-50 form-control" id="start" name="trip-start" value="" min="2023-01-01" max="2024-12-31">
    </div>

  <div class="col-auto mt-4">
    <button type="submit" class="btn btn-primary">Envoyer</button>
  </div>
</form> #}

<hr class="mt-3">

<br>
<br>
<div class="col-md-12 text-center">
		<a class="w-50 btn btn-lg btn-primary" href={{ path('accueil') }}>Revenir à l'accueil</a>
		
		<a class="w-50 btn btn-lg btn-primary mt-3" href={{path('app_logout')}}>Déconnexion</a>
	</div>

{% endblock %}
